local M = {}

function M.setup()
  -- local notify = require("mini.notify")
  -- notify.setup({
  --   lsp_progress = { enable = false },
  --   timeout = 3000,
  --   winblend = 0,
  -- })
end

return M
